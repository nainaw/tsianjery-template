name: Build Tsianjery

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Scribe
        run: |
          ruby tools/scribe.rb manuscripts/TelovolanaII.tsm
