name: Build Tsianjery

on:
  push:
    branches:
      - main

jobs:

  hello:

    runs-on: ubuntu-latest

    steps:

      - name: Run Scribe

        run: |
          ruby tools/scribe.rb TelovolanaII.tsm
